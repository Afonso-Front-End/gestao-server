# ============================================
# Torre de Controle - Configurações do Servidor
# ============================================
# Copie este arquivo para .env e configure as variáveis
# OU renomeie para .env

# ============================================
# MongoDB
# ============================================
MONGODB_URL=mongodb://localhost:27017
DATABASE_NAME=bdlogistica

# ============================================
# Servidor FastAPI
# ============================================
# Porta do servidor (mude se houver conflito com outras aplicações)
# IMPORTANTE: Se mudar aqui, atualize também o frontend!
PORT=8001

# Host do servidor (0.0.0.0 para aceitar conexões de qualquer IP)
HOST=0.0.0.0

# Modo debug (true/false)
# Em produção, deixe como false
DEBUG=false

# Nível de logging (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ============================================
# Segurança
# ============================================
# Rate limiting: requisições por minuto por IP
# 500 req/min é adequado para uso local com muitas requisições simultâneas
RATE_LIMIT_REQUESTS=500
RATE_LIMIT_WINDOW=60

# API Secret Key (chave secreta para autenticação)
# Gere uma chave segura usando: python -m app.core.api_key_generator
# Se deixar vazio, a autenticação por API Key será desabilitada (modo desenvolvimento)
API_SECRET_KEY=

# ============================================
# Notas Importantes
# ============================================
# Se mudar a PORT, atualize também:
# 1. app/vite.config.js (proxy target) OU app/.env (VITE_API_PORT)
# 2. app/src/utils/tauri-utils.js (backendPort) OU localStorage
#
# Para desenvolvimento local, geralmente PORT=8001 está OK
# Para produção, use uma porta específica (ex: 8080, 3001, etc)

